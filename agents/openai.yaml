interface:
  display_name: "LTspice Simulation v2"
  short_description: "Verified circuit diagrams, schematics, and LTspice runs"
  default_prompt: "Use $ltspice-sim-v2 to create my circuit diagram, verify its requirements, and deliver the editable LTspice schematic."

policy:
  allow_implicit_invocation: true
